# Development install. Runtime dependencies are declared in pyproject.toml —
# this file exists so `pip install -r requirements.txt` sets up a working repo
# checkout without duplicating the dependency list.
-e .[rfdetr,test]

# Optional model families (install as needed):
#   pip install -e .[paddleocr]   # paddleocr + ppstructure adapters
#   pip install -e .[easyocr]     # easyocr adapter
# detectron2 must be installed per platform:
#   https://detectron2.readthedocs.io/en/latest/tutorials/install.html
